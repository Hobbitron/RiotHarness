import { Block } from "./block";
export class Recommended {
    blocks: Array<Block>;
    champion: string	
    map: string	
    mode: string	
    priority: boolean	
    title: string	
    type: string
}